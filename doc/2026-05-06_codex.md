~/w/src/eubim/Codex/2026-05-06_codex.md

# OpenAI Codex CLI Creates Revit Add-in from Scratch

Gemini conversations executed in parallel with the codex session: 

- using openai codex on windows with deepseek llm -- https://gemini.google.com/share/9e29025d016e
- working with codex and revit api -- https://gemini.google.com/share/11d0ae58132a
- the entire codex cli session -- https://gemini.google.com/share/fe0508cb32ec

## Demo02

Prompts to Codex 2026-05-06:

0. launch in `C:\Users\j\w\src\eubim\Codex`
1. create a new folder named Codex and enter it
2. create a windows .net 8.0 class library C# project named Demo02. we will use it to implement a Revit 2026 C# .NET add-in project implementing an external command named CmdDemo02.
3. in the add-in manifest `VendorId` tag value, Autodesk recommends using a reverse domain name (e.g., com.autodesk) to ensure uniqueness. In this case, use The Building Coder web site URL `https://jeremytammik.github.io/tbc/a/` and the the vendor id to `<VendorId>io.github.jeremytammik</VendorId>`.
4. can you launch revit.exe for debugging?
5. i confirm that the external command CmdDemo02 installed and executed correctly. please install instrumentation code in the command so that it writes a log to the current development directory subfolder `C:\Users\j\w\src\eubim\Codex\Demo02\log` and logs its execution results there so that you can verify the execution yourself on the next run.
6. well done. it worked fine, right out of the box.
7. copy a transcript of the entire codex session today including all my prompts, your thought processes, confirmation requests and intermediate reports to the clipboard; /copy only copies the last response, but i would like a transcript of the entire conversation so far.

Result of prompts 4 and 5: Revit launched, add-in installed, launching the external command produces this TaskDialog:

```
	[Window Title]
	CmdDemo02 - Demo02

	[Main Instruction]
	CmdDemo02 is ready.
	Log: C:\Users\j\w\src\eubim\Codex\Demo02\log\20260506-153503-426-CmdDemo02.log

	[Close]
```

Result of prompt 7:

- C:\Users\j\w\src\eubim\Codex\Demo02_codex_session_transcript.txt
- ~/w/src/eubim/Codex/2026-05-06_codex_session_transcript.txt

This OpenAI Codex CLI session was executed using `/model 3. gpt-5.4-mini`, labelled 'Small, fast, and cost-efficient model for simpler coding tasks', with medium effort selected.

I can use that model and setting for free, afaik, linked to my free tier OpenAI ChatGPT account.

Token usage: total=396,217 input=376,743 (+ 1,858,304 cached) output=19,474 (reasoning 9,367)

To continue this session, run codex resume 019dfd55-2377-7f50-b2ac-d07a6c729ce7

## 2026-05-15:

Updated Codex:

1. Launch `codex resume 019dfd55-2377-7f50-b2ac-d07a6c729ce7`
2. Updating Codex via `npm install -g @openai/codex`...
3. Update ran successfully! Please restart Codex.
4. Relaunch `codex resume 019dfd55-2377-7f50-b2ac-d07a6c729ce7`

Prompts to Codex 2026-05-15:

I prefer implementing a naked Revit add-in from scratch.

Just a pure external command with no frills, that I can then refactor into snippets to implement a simple unit testing framework and autonomous agentic loop by calling test code from OnIdling, triggered by subsrbing to the Idling event in ApplicationInitialiszed.

Gemini:

OnStartup, ApplicationInitialized, Idling: https://gemini.google.com/share/1a03761cf1fa


1. refactor CmdDemo02.Execute by adding a helper function: implement a method `Execute2` that takes only the `Document`
  argument to work on. so, `Execute` determines the current document and passes that to `Execute2`, which does the
  actual work. then, subscribe to the ApplicationInitialized event during OnStartup, subscribe to Idling inside that
  event handler (named `OnApplicationInitialized`). in the Idling event handler `OnIdling`, unsubscribe immediately
  from Idling, and call CmdDemo02.Execute2 with the current document to automatically exercise the CmdDemo02
  functionality. install the updated add-in, launch revit, close revit again and verify that the entire sequence is
  called as expected by checking the log file generated.

interesting log entry:


• Ran dotnet build Demo02.csproj -c Debug.R26 -p:InstallRevitAddin=true; Start-Process 'C:\Program Files\Autodesk\Revit
  │ 2026\Revit.exe'; Start-Sleep -Seconds 25; Stop-Process -Name Revit -Force -ErrorAction SilentlyContinue;
  │ Get-ChildItem 'C:\Users\j\w\src\eubim\Codex\Demo02\log' | Sort-Object LastWriteTime -Descending | Select-Object
  │ … +1 lines
  └
    Welcome to .NET 9.0!
    … +1296 lines (ctrl 


Token usage: total=2,120,080 input=1,984,123 (+ 11,858,432 cached) output=135,957 (reasoning 58,356)
To continue this session, run codex resume 019dfd55-2377-7f50-b2ac-d07a6c729ce7

Prompts to Codex 2026-05-16:

› run /review on the current add-in implementation in `Codex`. pay attention to the external command separation into Execute and Execute2 and the automatic call to Execute2 on application startup via opening
  and empty BIM in the command line, implementing ApplicationsInitialized and Idling event hadlers. can we save that information, structure and architecture in a file for future reference? would that be a
  good way to make use of an `AGENTS.md` file?


› fix the defect number 1 that you list above. keep the changes minimal.

> save AGENTS.md describing the current structure for future implementations

› implement a second external command similar to the first existing one. name the new command module CmdLittleHouse. it should ensure two levels exist in the model at elevation 0 and 3000 mm, generate four
  walls forming a 3 m x 4 m rectangle between the lower and the upper level, add a floor below the walls, a sloped footprint roof on top, a door centered in one of the two long walls, a window centered in
  each of the other three, and define a roof in the interior of the four walls. The Building Coder implemented this exact command in the beginning of its existance, around 2010, so you can use that code as a
  reference.

› implement a second external command similar to the first existing one. name the new command module CmdLittleHouse. it should ensure two levels exist in the model at elevation 0 and 3000 mm, generate four
  walls forming a 3 m x 4 m rectangle between the lower and the upper level, add a floor below the walls, a sloped footprint roof on top, a door centered in one of the two long walls, a window centered in
  each of the other three, and define a roof in the interior of the four walls. The Building Coder implemented this exact command in the beginning of its existance, around 2010, so you can use that code as a
  reference. when finished, compile, sign the assembly, install it, launch revit with the empty BIM RVT on the command line, ensure that the little house Execute2 is executed, and verify that all the expected
  elements are created, e.g., by using a filtered element collector to retrieve them all after the execution. produce a full log file and a human readable report in markdown format.


› use a filtered element collector to retrieve the existing door and window families in the empty BIM. there are some default doos and windows present. you do not need to care about external RFA files. you do
  not need to load anything. everything you need is already there. The Building Coder samples on github include all the code needed to create the little house model. look at the external command
  `Lab2_0_CreateLittleHouse` in https://github.com/jeremytammik/AdnRevitApiLabsXtra/blob/master/XtraCs/Labs2.cs in the repository `github.com/jeremytammik/AdnRevitApiLabsXtra`. don't make sucgh a fuss,
  everything is already there. just copy it and adapt to revit 2026. also, there is no need for two roofs. we just wnat one. the outer edge of the floor should match the outer edges of the walls. the roof
  should extend over and beyond the outer edges of the walls.


› the door is visible now. the only remaining problems are the the roof overhang is a bit too large and that the walls do not extend to the roof. the overhang is 470 mm. 300 is sufficient. the walls can be
  automatically extended upwards to join the roof using the command modify wall > attach top

› copy a transcript of the entire codex session today including all my prompts, confirmation requests and intermediate reports to a text file in `C:\Users\j\w\src\eubim\Codex\2026-05-
  16_codex_session_transcript.txt`. i would like a transcript of the entire conversation so far.

Token usage: total=4,786,136 input=4,583,639 (+ 23,703,808 cached) output=202,497 (reasoning 77,160)
To continue this session, run codex resume 019dfd55-2377-7f50-b2ac-d07a6c729ce7

